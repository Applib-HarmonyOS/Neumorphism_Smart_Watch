<div class="neumorph_container" style="width: {{width}}; height: {{height}}; border-radius: {{border}};">
    <div class="neumorph_innercontainer neumorph_input" style="border-radius: {{border}};">
        <image if="{{icon}}" class="icon" src="{{icon}}"></image>
        <input type="{{ type }}" maxlength="{{ maxlength }}" placeholder="{{ placeholder }}" ></input>
    </div>
</div>